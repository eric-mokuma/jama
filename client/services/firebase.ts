// Firebase service for chat and notifications
// TODO: Add your Firebase config and initialization here

export const sendMessage = (chatId: string, message: string) => {
  // Placeholder for sending a message via Firebase
}

export const subscribeToMessages = (
  chatId: string,
  callback: (msg: any) => void
) => {
  // Placeholder for real-time message subscription
}
